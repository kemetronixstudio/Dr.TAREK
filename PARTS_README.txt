Extract all three zip files into the same parent folder.
Allow your unzip tool to merge folders and overwrite duplicates if prompted.
You should end up with one folder named Dr.TAREK-main containing the full patched project.
